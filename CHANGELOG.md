# Horae 更新日誌 / Changelog

> 最近版本的更新內容請檢視 [README.zh-TW.md](README.zh-TW.md)

---

## v1.15.1-minijin.1 — Candidate Code Freeze

> 狀態：候選程式碼已凍結，尚未執行最終驗證。實機驗證日期：**待最終驗證補登**。
> 下一步：一次執行完整驗證、SillyTavern 1.18.0 實機矩陣與 Grok 獨立審計。

### Minijin Fork：回覆後輔助 API 結算

- 新增預設關閉的「回覆後輔助 API 結算」開關；關閉時保留上游行為。
- 開啟時主模型提示詞保留 Horae 資料與召回內容，但移除標籤輸出規則。
- 主回覆顯示後，由既有輔助 API 的獨立序列交易非同步擷取並寫回原版 Horae 標籤格式。
- 輔助 API 失敗不會回退主 API；正文保持可用並顯示錯誤。
- 下一輪主請求最多等待對應結算 2 秒，避免無限阻塞。
- 以 chat／message／swipe／正文／meta／歷史脈絡指紋約束寫回歸屬；過期結果不得覆蓋重骰、編輯或手動資料。
- 補齊聊天切換回放、pristine greeting sidecar、overswipe、swipe 刪除、純標籤編輯與 system/tool 排除。
- 歷史重抽以目標樓前的來源重建場景、關係、待辦與 RPG，不讀取 root 中的未來聚合。
- 修正 SillyTavern 1.18.0 CORS 代理路徑、角色卡 CSRF 與 Port API 憑證遮罩；詳細維護邊界見 [FORK_NOTES.md](FORK_NOTES.md)。

---

## v1.15.0

### 向量記憶快照

- **「建立攜帶記憶新對話」攜帶向量記憶**：confirm 彈窗內勾選後，源對話的向量索引會以 `.json` / `.json.gz` 快照形式打包並自動掛到新對話名下用於召回，同時下載一份本地備份。輕負擔，手機端 / 雲酒館均可使用。
- **鏈式傳遞**：源對話已掛載的祖先快照也會隨之轉移到新對話名下，多代「建立攜帶記憶新對話」不再丟失早期記憶。toast 會顯示轉移的祖先快照數量。
- **快照管理 UI**：向量設定區新增快照列表，可手動匯入 `.json` / `.json.gz` 快照、檢視維度與匯出時間、移除單條快照。維度與當前模型不一致的快照在掛載階段自動跳過。
- IndexedDB 增加 `memorySnapshots` store。

---

## v1.14.0

### 輔助 API

- 新增獨立「輔助 API」設定區塊，可填寫 OpenAI 相容端點、API Key 與模型，並按用途選擇是否啟用。
- 輔助 API 使用範圍拆分為三組：
  - AI 分析、樓層/底部欄魔術棒、傳送前自動補全上一條 AI 訊息時間線。
  - 自動總結、二次總結、AI 智慧補全。
  - 手動多選壓縮，預設關閉；若輔助 API 破限不足，可關閉此項改走酒館原生通道並攜帶主預設。
- 輔助 API 請求改為序列佇列，避免自動總結、魔術棒、智慧補全等後臺任務併發觸發副端點限流。
- 新增「輔助 API 失敗時回退主 API」選項，預設關閉，避免副端點失敗後繼續消耗主 API 速率。
- 舊版自動總結副 API 配置會自動遷移到新的輔助 API 欄位，並清空舊欄位，避免重複儲存金鑰。
- 輔助 API 的地址、金鑰、模型不會寫入 Horae 配置檔匯出白名單，防止分享配置時洩露。

---

## v1.13.0

### Horae Port

- 新增 Horae Port 系統，開放 `window.Horae.registerPort()` / `unregisterPort()` / `refreshPorts()`，支援外部元件掛載到底部欄、狀態頁、抽屜分頁、訊息面板與 RPG HUD。
- 新增 Data Provider 機制，開放 `registerDataProvider()` / `unregisterDataProvider()`，第三方指令碼可按需橋接自己的狀態資料；Horae 不內建任何外部變數系統依賴。
- 埠上下文提供 Horae 聚合狀態、RPG 快照、訊息 meta、Provider 快照與常用 helper，支援 `render` / `update` / `dispose` 生命週期。
- 新增 `portApiVersion`，為後續埠協議相容提供穩定判斷入口。

### 文件

- 新增 `Horae端口系統說明.md`，向擴充功能作者說明端口 API、插槽、生命週期、Provider、主題約定與安全邊界。
- 新增 `Horae狀態欄接入說明.md`，向角色卡／正則狀態欄作者說明如何透過 `window.parent.Horae` 讀取 Horae 狀態。

---

## v1.12.0

- 新增公開只讀 API（`window.Horae`）：`isEnabled()`、`getLatestState()`、`getEvents()`、`getSettings()`、`version`，供其他擴充套件和預設指令碼讀取 Horae 狀態；設定變更通過 `eventSource` 廣播 `horae:settingsChanged` 事件。
- 基於 baibai-git 的 PR #5 整合。
- 針對 issue #4 的需求新增 Gemini Embedding API 相容。
- 向量召回查詢改為單次 Embedding 的合併格式：
  - `[當前情境] <time/location/characters/events from latest AI state>`
  - `[玩家輸入] <latest user input>`
- Rerank 查詢同步使用合併後的召回查詢，不再只使用玩家輸入文本。
- 狀態查詢會附帶時間資訊，優先使用最新 AI 回合時間，缺失時回退到聚合狀態時間。
- 移除向量召回執行時的 fallback 路徑，不再用上一條 AI 回覆重試召回。
- 新增向量召回引數預設，可在小模型、大模型、Rerank 建議和使用者自定義引數之間快速切換；預設回到小模型建議（Top-K 5 / 閾值 0.72）。
- 新增有效召回閾值的執行日誌，便於除錯召回效果。
- 修復樓層 AI 分析時會觸發向量召回的問題。
- 劇情軌跡注入支援“原版合併注入”和“獨立前置注入”雙模式，預設保留原版合併注入。

---

## v1.10.0

全面重構 RPG 系統，新增 6 個 RPG 子模組、據點經營系統、裝備多種族模板等功能。

### RPG 模式

- **子模組獨立開關**：裝備欄、聲望、等級、貨幣、據點各為獨立 checkbox，按需組合。關閉時零 Token 消耗
- **角色卡標籤分頁**：角色詳情區改為分頁標籤（屬性/技能/裝備/聲望/貨幣），僅顯示已開啟的模組
- **只顯示在場角色**：頂部角色卡和底部 HUD 根據 `characters_present` 過濾，離場角色資料保留但不顯示

### 新增功能

- **等級/經驗值系統**：AI 通過 `level:` / `xp:` 標籤輸出等級變化。經驗值公式：升級所需 = 等級 × 100，等級差 ≥10 的低階挑戰僅得 1 點經驗
- **貨幣系統**：自定義幣種和兌換比率，每個幣種可設定 emoji 圖示，AI 通過 `currency:歸屬|幣名=±增量` 修改
- **聲望系統**：自定義勢力聲望分類和數值範圍，支援細項（子維度）
- **裝備欄系統**：按角色獨立格位，內建人類/獸人/有翼族/馬人/拉彌亞/惡魔六種預設模板，支援自存模板
- **據點/基地系統**：多層級樹形結構，AI 通過 `base:` 標籤自動更新，支援手動管理和匯出/匯入

---

## v1.9.0

### 自動摘要 & 副 API

- **副 API 多輪結構改造**：重構摘要請求訊息結構，採用 system/assistant/user 多輪交替格式，大幅提升 NSFW 內容摘要通過率
- **模型下拉選單**：副 API、Embedding、Rerank 三處模型設定均從文本輸入改為下拉選單
- **自動摘要警告最佳化**：不再因部分樓層缺少時間戳就完全阻止摘要

### 向量記憶

- **全文剔除標籤**：向量設定中新增「全文剔除標籤」輸入框，避免小劇場/旁白等非劇情內容被 AI 召回
- **番外訊息隔離**：標記為「番外」的訊息不再被向量索引和召回

---

## v1.8.0

- **RPG 模式**：開啟後 AI 通過 `<horaerpg>` 標籤輸出角色屬性和技能變化，與現有 `<horae>` 標籤互不干擾
  - **自定義屬性條**：預設 HP/MP/SP 三條，使用者可自由增刪、修改顯示名稱和顏色（如改 MP 為「靈力」、新增「精力值」「慾望值」等）
  - **狀態異常追蹤**：支援昏眩、流血、冰凍、灼燒、石化等數十種狀態關鍵詞，自動匹配差異化圖示顯示
  - **技能系統**：記錄技能歸屬、等級和效果描述，技能與 NPC 角色通過編號聯動，支援手動新增和 AI 自動生成
  - **底部 HUD 狀態列**：訊息面板上方渲染在場角色的簡易狀態條，顯示屬性條和異常狀態圖示，自動跟隨底部欄的寬度和偏移設定
  - **RPG 分頁**：頂部面板新增 RPG 標籤頁，展示屬性條配置、各角色詳細屬性和狀態列表、技能列表
  - **條件注入**：關閉 RPG 模式時不注入任何提示詞，零 Token 消耗；傳送屬性條和技能列表可獨立開關
- **RPG 面板美化**：自助美化工具新增「RPG 狀態列」區域，支援設定背景色和透明度。RPG HUD 納入主題系統，跟隨日夜模式和自定義主題變數

---

## v1.7.0

- **向量記憶 (Beta)**：基於本地 AI 模型的智慧回憶系統，專為搭配「自動摘要 & 隱藏」設計。當對話中涉及歷史事件時，外掛會自動從被隱藏的舊訊息中精準召回相關時間線片段，注入上下文供 AI 參考
  - **全程本地運算**：使用瀏覽器內建 Web Worker 執行輕量 AI 模型，不消耗額外 API 額度
  - **雙模型可選**：`bge-small-zh-v1.5`（中文最佳化）和 `multilingual-e5-small`（多語言支援）
  - **智慧檢索**：結合意圖識別、結構化查詢和語義搜尋三層檢索策略
  - **全文回顧**：高置信度的召回結果可傳送原始正文，讓 AI 獲得完整的敘事細節

---

## v1.6.0

- **自助美化工具**：視覺化美化面板。色相條、飽和度/亮度/日夜模式滑桿、圖片裝飾。所有修改即時預覽，可匯出 JSON 分享
- **新手導航**：首次使用自動觸發互動式教學
- **副 API 破限注入**：自動摘要的獨立 API 請求現在會自動注入 SillyTavern 的 jailbreak 提示詞和 `max_tokens` 引數

---

## v1.5.0

### 新增功能

- **情緒/心理狀態追蹤**：追蹤在場角色的情緒變化。Token 消耗極低——AI 僅在情緒發生明顯變化時才輸出 `mood:` 標籤
- **關係網路**：記錄角色之間的關係（朋友、戀人、上下級、宿敵等）。展示完全由外掛從資料庫讀取渲染，不消耗 AI 輸出 Token
- **場景記憶**：記錄地點的固定物理特徵，保持跨回合場景描寫一致。智慧檢索——僅傳送匹配到的那一條描述
- **自動摘要 & 隱藏**：超過閾值自動觸發全文摘要，原始樓層 `/hide` 省 Token
- **獨立 API（真並行）**：自動摘要可配置獨立的 OpenAI 相容端點，實現與 AI 回覆的真並行

### 改進

- **全域性表格資料按卡分離**
- **智慧記憶壓縮**：多選模式下可將多條事件交給 AI 壓縮合併為一條摘要

---

## v1.4.0

- **好感度小數點支援**
- **頂部圖示開關**
- **時間線插入功能**：長按時間線事件彈出操作選單，支援在上方/下方新增事件或插入摘要頁
- **表格填寫提示詞自定義**
- **表格 AI 理解增強**：傳送給 AI 的表格資料帶有座標標註
- **表格資料清理修復**

---

## v1.3.0

- **全域性表格**：自定義表格新增「全域性」和「本地」兩種作用域
- **行列鎖定**：右鍵/長按表格單元格可鎖定行、列或單個格子
- **主題系統**：內建「櫻花粉」「森林綠」「海洋藍」三套預設主題
- **自助美化**：新增 `THEME.md` 文件
- **日夜模式**
- **自定義 CSS**
- **訊息面板寬度**
- **正則置底**
- **時間線多選**

---

## v1.2.0

- **自定義提示詞**：設定中新增提示詞編輯器
- **AI 摘要功能新增**：可中斷生成，新增 NPC 和好感度勾選項
- **摘要審閱彈窗**
- **好感度顯示開關**
- **好感度刪除**

---

## v1.1.0

- **AI 智慧摘要**：批次分析歷史訊息，自動提取劇情事件和物品
- **Token 計數器**
- **swipe 適配**
- **美化相容**
- **正則修改**
